import { PhotoVideoForm } from '@/components/forms/photo-video-form'

export const metadata = {
  title: 'Photo / Video Application — Windy City Raves',
}

export default function PhotoVideoApplyPage() {
  return <PhotoVideoForm />
}
